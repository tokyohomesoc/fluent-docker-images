<source>
  type twitter

  consumer_key        "#{ENV['TWITTER_CONSUMER_KEY']}"        # Required
  consumer_secret     "#{ENV['TWITTER_CONSUMER_SECRET']}"     # Required
  oauth_token         "#{ENV['TWITTER_ACCESS_TOKEN']}"        # Required
  oauth_token_secret  "#{ENV['TWITTER_ACCESS_TOKEN_SECRET']}" # Required
  tag                 twitter.sampling                        # Required
  timeline            sampling                                # Required (tracking or sampling or location or userstream)
  keyword             "#{ENV['TWITTER_KEYWORD']}"             # Optional (keyword has priority than follow_ids)
  lang                "#{ENV['TWITTER_LANG']}"                # Optional
  output_format       flat                                    # Optional (nest or flat or simple[default])
  flatten_separator   _                                       # Optional
</source>